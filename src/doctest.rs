/// Adds one to an input number.
///
/// # Examples
///
/// ```
/// let arg = 5;
/// let answer = rustsandbox::doctest::add_one(arg);
/// assert_eq!(answer, 6);
/// ```
pub fn add_one(x: u32) -> u32 {
    x + 1
}
